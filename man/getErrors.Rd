% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils.R
\name{getErrors}
\alias{getErrors}
\title{Collect and handle errors in a standardized way}
\usage{
getErrors(e = NULL, err = NULL, immediate. = TRUE)
}
\arguments{
\item{e}{A character vector of new error messages to report.}

\item{err}{A character vector of existing errors (can be \code{NULL} or empty).}

\item{immediate.}{Logical. If \code{TRUE} (default), throws an error immediately
with the contents of \code{e}. If \code{FALSE}, returns all errors invisibly.}
}
\value{
If \code{immediate.} is \code{FALSE}, returns a combined character vector of
\code{err} and \code{e} invisibly. If \code{immediate.} is \code{TRUE}, raises an error.
}
\description{
\code{getErrors()} helps accumulate error messages during validity checks
or other internal validations. It supports returning the accumulated
errors or stopping immediately.
}
\examples{
# Accumulate errors without stopping
err <- getErrors("First error", immediate. = FALSE)
err <- getErrors("Second error", err = err, immediate. = FALSE)
print(err)

# getErrors("Something went wrong")

}
